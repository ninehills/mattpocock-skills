---
name: handoff
description: 将当前对话压缩为交接文档，供另一个代理接手。
argument-hint: "下一次会话将用于什么？"
---

编写一份交接文档，总结当前对话，以便一个全新的代理可以继续工作。将其保存到 `mktemp -t handoff-XXXXXX.md` 生成的路径（在写入之前先读取该文件）。

建议下一个会话使用的技能（如果有）。

不要重复已存在于其他工件中的内容（PRD、计划、ADR、issue、提交、diff）。改为通过路径或 URL 引用它们。

如果用户传递了参数，将其视为下一个会话重点的描述，并相应地调整文档。
